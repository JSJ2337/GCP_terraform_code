# 프로젝트 설정 예제
# 이 파일을 terraform.tfvars로 복사하고 실제 값을 입력하세요
# 중요: terraform.tfvars 파일은 버전 관리에 커밋하지 마세요

project_id      = "your-project-id"
project_name    = "Default Template Production"
folder_id       = "folders/123456789012"
billing_account = "ABCDEF-123456-GHIJKL"

labels = {
  environment = "prod"
  project     = "default-templet"
  managed_by  = "terraform"
  cost_center = "gaming"
}

# 활성화할 API 목록
apis = [
  "compute.googleapis.com",
  "iam.googleapis.com",
  "servicenetworking.googleapis.com",
  "logging.googleapis.com",
  "monitoring.googleapis.com",
  "cloudkms.googleapis.com",
  "storage.googleapis.com",
  "cloudresourcemanager.googleapis.com"
]

# 예산 설정
# 참고: Budget API는 특별한 할당량 프로젝트 설정이 필요합니다.
# 초기 배포 시 인증 문제를 피하려면 false로 설정하세요.
# 나중에 GCP 콘솔에서 활성화할 수 있습니다: Billing → Budgets & alerts
enable_budget   = false
budget_amount   = 1000
budget_currency = "USD"

# 로그 보관 기간 (일)
log_retention_days = 30

# 선택사항: 로그 암호화용 CMEK 키
# cmek_key_id = "projects/YOUR_PROJECT/locations/REGION/keyRings/KEYRING/cryptoKeys/KEY"

# 참고: 프로덕션 환경에서 프로젝트 삭제 방지가 필요한 경우
# modules/project-base/main.tf의 lifecycle 블록 주석을 해제하세요
