# 작업 이력 - 2025-11-17

**작업자**: Codex  
**브랜치**: 433_code

---

## 🎯 작업 요약
- Memorystore 모듈을 STANDARD/Enterprise 겸용으로 재구성하고 Terragrunt 템플릿/환경 tfvars를 Enterprise 기본값으로 정비
- Cloud SQL 모듈의 읽기 복제본 생성 오류를 해결하고 tfvars 예시/환경에 기본 리드 노드를 추가
- 10-network 레이어에서 Memorystore Enterprise용 Service Connection Policy를 자동으로 만들도록 인프라/문서를 업데이트

---

## ✅ 완료된 작업
1. **Memorystore Enterprise 지원**
   - `modules/memorystore-redis`에 `google_redis_cluster` 경로를 추가해 Enterprise/Enterprise Plus 티어에서 PSC 기반 Redis Cluster를 생성
   - `enterprise_*` 입력(노드 타입, 암호화, Redis 설정)을 신설하고 STANDARD/BASIC 경로와 분기
   - 출력/README를 Enterprise 동작(PSC 연결 정보, replica/shard 요구 사항) 기준으로 재작성
2. **템플릿/환경 tfvars 반영**
   - `proj-default-templet/65-cache`와 `environments/LIVE/jsj-game-m/65-cache`의 tfvars·예시 파일을 Enterprise 설정(tier, replica_count, shard_count, connect_mode)으로 수정
   - Cloud SQL 템플릿과 jsj-game-m tfvars에 기본 읽기 복제본 예시를 추가하고 작업 가이드를 README에 명시
3. **Cloud SQL 모듈 보완**
   - `modules/cloudsql-mysql`에서 읽기 복제본 `replica_configuration`을 조건부 블록으로 전환하고, `ip_configuration`이 선택적으로 private_network/IPv4를 상속하도록 수정
   - 기본 설정만 입력했을 때도 읽기 복제본이 생성되도록 예시 tfvars를 재작성
4. **Memorystore PSC 정책 자동화**
   - `proj-default-templet/10-network`와 jsj-game-m 환경에 `google_network_connectivity_service_connection_policy` 리소스를 추가해 Enterprise가 요구하는 PSC 정책을 Terraform으로 생성
   - 관련 변수/README를 확장하고 tfvars에 기본값을 활성화
5. **문서화/변경 기록**
   - README, CHANGELOG, work history에 이번 변경 내용을 정리하고 Enterprise 사용 시 선행 조건(PSC 정책)을 명확히 기재

---

## 📌 메모
- Memorystore Enterprise를 사용하려면 10-network 레이어에서 `enable_memorystore_psc_policy = true`로 설정하고, PSC가 사용할 서브넷/리전을 올바르게 지정해야 합니다.
- Cloud SQL 읽기 복제본은 `failover_target`을 명시하지 않으면 자동으로 읽기 전용으로만 생성됩니다. 필요 시 해당 필드를 true로 설정하세요.
- Jenkins 파이프라인에서는 10-network → 60-database → 65-cache 순으로 Terragrunt apply를 실행해 PSC 정책이 존재하는지 먼저 확인해야 합니다.

---

## 🔗 오늘 커밋
- `chore: Redis Enterprise 구성 적용`
- `chore: Cloud SQL 읽기 노드 추가`
- `feat: Memorystore Enterprise 지원`
- `fix: Cloud SQL replica block & Redis labels`
- `fix: optional Cloud SQL IP blocks`
- `fix: Cloud SQL replica private network fallback`
- `feat: Memorystore PSC Service Connection Policy`

