# Work History - 2025-12-01

## 작업 개요
gcby 프로젝트의 DNS Peering 설정, 3-tier 네트워크 격리, Cloud SQL PSC Endpoint 전환

## 주요 작업 내역

### 1. DNS Peering 설정 (mgmt ↔ gcby)

**요구사항:**
- mgmt VPC에서 gcby VM의 DNS 이름으로 접근 가능하도록 설정
- DNS 접근은 mgmt에서만 가능 (단방향)
- jenkins, bastion에서 gcby-gs01, gcby-gs02로 접근

**구현:**

#### 1.1 VPC Peering 구성
**파일:** `bootstrap/10-network/main.tf`, `environments/LIVE/gcp-gcby/10-network/main.tf`

```hcl
# mgmt → gcby VPC Peering
resource "google_compute_network_peering" "mgmt_to_gcby" {
  name         = "peering-mgmt-to-gcby"
  network      = google_compute_network.mgmt_vpc.self_link
  peer_network = "projects/gcp-gcby/global/networks/gcby-live-vpc"

  import_custom_routes = true
  export_custom_routes = true
}

# gcby → mgmt VPC Peering
resource "google_compute_network_peering" "gcby_to_mgmt" {
  name         = "peering-gcby-to-mgmt"
  network      = module.net.vpc_self_link
  peer_network = "projects/delabs-gcp-mgmt/global/networks/delabs-gcp-mgmt-vpc"

  import_custom_routes = true
  export_custom_routes = true
}
```

#### 1.2 DNS 레코드 추가
**파일:** `bootstrap/12-dns/layer.hcl`

```hcl
dns_records = {
  # mgmt 프로젝트 VM들
  "jenkins" = {
    type    = "A"
    ttl     = 300
    rrdatas = ["10.250.10.7"]
  }
  "bastion" = {
    type    = "A"
    ttl     = 300
    rrdatas = ["10.250.10.6"]
  }

  # gcp-gcby 프로젝트 VM들 (VPC Peering 필요)
  "gcby-gs01" = {
    type    = "A"
    ttl     = 300
    rrdatas = ["10.10.11.3"]
  }
  "gcby-gs02" = {
    type    = "A"
    ttl     = 300
    rrdatas = ["10.10.11.6"]
  }
}
```

#### 1.3 DNS Peering Zone 생성
**파일:** `environments/LIVE/gcp-gcby/12-dns/terraform.tfvars`

```hcl
zone_name   = "gcby-dns-peering-to-mgmt"
dns_name    = "delabsgames.internal."
description = "DNS Peering to mgmt VPC for internal name resolution"
visibility = "private"
private_networks = []
peering_network = "projects/delabs-gcp-mgmt/global/networks/delabs-gcp-mgmt-vpc"
dns_records = []  # DNS 레코드는 mgmt에서만 관리
```

**커밋:** a74b413

---

### 2. DNS Layer 번호 변경 (75-dns → 12-dns)

**문제:**
- 초기에 75-dns로 생성 (Phase 8 위치)
- DNS는 Network 다음에 와야 논리적으로 맞음

**해결:**
- 모든 프로젝트의 75-dns를 12-dns로 변경
  - gcp-gcby
  - jsj-game-n
  - proj-default-templet
- Jenkinsfile Phase 재편성
  - Phase 3: 12-dns (DNS Peering)
  - Phase 4-8: 기존 Phase 3-7 재배치
- README 및 모든 docs 일괄 업데이트

**커밋:** 여러 커밋 (force push로 amend)

---

### 3. SSH 접근 제어 개선

**문제:**
- bastion에서 gcby VM으로 SSH 접속 불가
- 방화벽 규칙이 IAP만 허용 (35.235.240.0/20)
- mgmt VPC (10.250.10.0/24)에서 접근 불가

**해결:**

#### 3.1 mgmt VPC SSH 방화벽 규칙 추가
**파일:** `environments/LIVE/gcp-gcby/10-network/terraform.tfvars`

```hcl
{
  name           = "allow-ssh-from-mgmt"
  direction      = "INGRESS"
  ranges         = ["10.250.10.0/24"] # mgmt VPC subnet
  allow_protocol = "tcp"
  allow_ports    = ["22"]
  target_tags    = ["ssh-from-mgmt"]
  description    = "Allow SSH from mgmt VPC (jenkins, bastion)"
}
```

#### 3.2 SSH 태그 분리
**이전:** `ssh-allowed` (모든 SSH 접근)
**이후:**
- `ssh-from-iap`: IAP tunnel 접근 (35.235.240.0/20)
- `ssh-from-mgmt`: mgmt VPC 직접 접근 (10.250.10.0/24)

**파일:** `environments/LIVE/gcp-gcby/50-workloads/terraform.tfvars`

```hcl
"gcby-gs01" = {
  tags = ["game-server", "ssh-from-iap", "ssh-from-mgmt", "private-zone"]
}
"gcby-gs02" = {
  tags = ["game-server", "ssh-from-iap", "ssh-from-mgmt", "private-zone"]
}
```

**커밋:** a74b413

---

### 4. DB Subnet → PSC 전용 대역 전환

**요구사항:**
- 기존 DB subnet (10.10.12.0/24)을 제거
- 동일 대역을 PSC 전용 IP 범위로 사용

**구현:**

#### 4.1 Network Module PSC 주소 지정 기능 추가
**파일:** `modules/network-dedicated-vpc/variables.tf`

```hcl
variable "private_service_connection_address" {
  description = "PSC IP 주소 (예: 10.10.12.0). 비우면 자동 할당"
  type        = string
  default     = ""
}
```

**파일:** `modules/network-dedicated-vpc/main.tf`

```hcl
resource "google_compute_global_address" "private_service_connect" {
  address       = length(trimspace(var.private_service_connection_address)) > 0 ? var.private_service_connection_address : null
  prefix_length = var.private_service_connection_prefix_length
  # ...
}
```

#### 4.2 gcby/10-network 설정 업데이트
**파일:** `environments/LIVE/gcp-gcby/10-network/terraform.tfvars`

**Before:**
```hcl
additional_subnets = [
  { cidr = "10.10.10.0/24" },  # DMZ
  { cidr = "10.10.11.0/24" },  # Private
  { cidr = "10.10.12.0/24" }   # DB
]

# PSC 자동 할당 (10.201.3.0/24)
```

**After:**
```hcl
additional_subnets = [
  { cidr = "10.10.10.0/24" },  # DMZ
  { cidr = "10.10.11.0/24" }   # Private
]

# PSC 명시적 지정
private_service_connection_address = "10.10.12.0"
private_service_connection_prefix_length = 24
```

#### 4.3 DB 관련 방화벽 규칙 삭제
- allow-db-internal (10.10.12.0/24 내부 통신)
- allow-private-to-db (Private → DB 통신)

**커밋:** 1c18375

---

### 5. Cloud SQL PSC Endpoint 전환 (3-tier 격리)

**문제:**
- 기존 VPC Peering 방식: 전체 VPC에서 Cloud SQL 접근 가능
- DMZ zone에서 DB 직접 접근 가능 (보안 취약)
- 3-tier 아키텍처 위배

**해결: PSC Endpoint 방식으로 전환**

#### 5.1 10-network: Cloud SQL Service Connection Policy 추가
**파일:** `environments/LIVE/gcp-gcby/10-network/variables.tf`

```hcl
variable "enable_cloudsql_psc_policy" {
  type        = bool
  description = "Cloud SQL용 Service Connection Policy 생성 여부"
  default     = false
}

variable "cloudsql_psc_region" {
  type        = string
  description = "Cloud SQL PSC 정책을 생성할 리전 (비워두면 region_primary 사용)"
  default     = ""
}

variable "cloudsql_psc_subnet_name" {
  type        = string
  description = "Cloud SQL PSC에서 IP를 할당할 서브넷 이름 (기본: private_subnet_name)"
  default     = ""
}

variable "cloudsql_psc_connection_limit" {
  type        = number
  description = "Cloud SQL PSC 서비스 연결 정책에서 허용할 최대 연결 수"
  default     = 5
}
```

**파일:** `environments/LIVE/gcp-gcby/10-network/main.tf`

```hcl
# Cloud SQL Private Service Connect Policy
resource "google_network_connectivity_service_connection_policy" "cloudsql_psc" {
  count         = var.enable_cloudsql_psc_policy ? 1 : 0
  project       = var.project_id
  location      = local.cloudsql_psc_region
  name          = local.cloudsql_psc_policy_name
  service_class = "gcp-cloud-sql"
  network       = "projects/${var.project_id}/global/networks/${module.naming.vpc_name}"

  psc_config {
    subnetworks = [local.cloudsql_psc_subnet_self_link]  # Private subnet만!
    limit       = var.cloudsql_psc_connection_limit
  }

  depends_on = [
    module.net,
    time_sleep.wait_networkconnectivity_api
  ]
}
```

**파일:** `environments/LIVE/gcp-gcby/10-network/terraform.tfvars`

```hcl
# VPC Peering 방식 비활성화
enable_private_service_connection = false

# Cloud SQL PSC Endpoint 활성화
enable_cloudsql_psc_policy = true
cloudsql_psc_connection_limit = 5  # Master + Read Replicas
```

#### 5.2 modules/cloudsql-mysql: PSC 지원 추가
**파일:** `modules/cloudsql-mysql/variables.tf`

```hcl
variable "enable_psc" {
  type        = bool
  description = "Private Service Connect 활성화 (true: PSC Endpoint, false: VPC Peering)"
  default     = false
}
```

**파일:** `modules/cloudsql-mysql/main.tf`

```hcl
settings {
  ip_configuration {
    ipv4_enabled = var.ipv4_enabled

    # PSC 방식: psc_enabled = true, private_network = null
    # VPC Peering 방식: private_network 사용
    private_network = var.enable_psc ? null : (length(trimspace(var.private_network)) > 0 ? var.private_network : null)

    # PSC Endpoint 활성화
    psc_enabled = var.enable_psc
  }
}
```

#### 5.3 60-database: PSC 활성화
**파일:** `environments/LIVE/gcp-gcby/60-database/terraform.tfvars`

```hcl
# Network configuration
ipv4_enabled = false  # No Public IP
enable_psc   = true   # PSC Endpoint (Private subnet only access)
```

**커밋:** 6463e4c

---

## 네트워크 아키텍처 변화

### Before (VPC Peering 방식)
```
Cloud SQL (10.201.3.2)
  ↑
  | VPC Peering (Private Service Connection)
  | → 전체 VPC에서 접근 가능
  |
gcby VPC
  ├─ DMZ zone (10.10.10.0/24) ✅ 접근 가능 (보안 취약!)
  ├─ Private zone (10.10.11.0/24) ✅ 접근 가능
  └─ mgmt VPC (10.250.10.0/24) ✅ 접근 가능
```

### After (PSC Endpoint 방식)
```
Cloud SQL (PSC Endpoint)
  ↑
  | Service Connection Policy
  | → Private subnet에만 Endpoint 생성
  |
gcby VPC
  ├─ DMZ zone (10.10.10.0/24) ❌ 접근 불가 (3-tier 격리)
  ├─ Private zone (10.10.11.0/24) ✅ 접근 가능
  └─ mgmt VPC (10.250.10.0/24) ✅ 접근 가능 (VPC Peering 통해)
```

### DNS 구조
```
mgmt VPC (delabs-gcp-mgmt-vpc)
  ├─ DNS Zone: delabsgames.internal.
  │   ├─ jenkins: 10.250.10.7
  │   ├─ bastion: 10.250.10.6
  │   ├─ gcby-gs01: 10.10.11.3
  │   └─ gcby-gs02: 10.10.11.6
  │
  └─ VPC Peering ←→ gcby VPC (gcby-live-vpc)
                       └─ DNS Peering Zone
                          └─ delabsgames.internal. → mgmt VPC
```

---

## Jenkins 적용 순서

### 1단계: DNS 및 Network 재구성
**Job:** `delabs_bootstrap`
**Layers:**
- `10-network` apply (mgmt → gcby VPC Peering)
- `12-dns` apply (gcby VM DNS 레코드)

**Job:** `(LIVE) gcp-gcby`
**Layers:**
- `10-network` apply
  - gcby → mgmt VPC Peering
  - Cloud SQL Service Connection Policy 생성
  - VPC Peering 방식 PSC 제거
- `12-dns` apply (DNS Peering Zone)
- `50-workloads` apply (VM 태그 업데이트)

### 2단계: Cloud SQL 재생성 (중요!)
**Job:** `(LIVE) gcp-gcby`
**Layer:** `60-database`

⚠️ **주의:** 기존 Cloud SQL을 삭제하고 재생성해야 합니다.

**절차:**
1. **백업 확인:**
   ```bash
   gcloud sql backups list --instance=gcby-live-gdb-m1 --project=gcp-gcby
   ```

2. **Destroy:**
   - Jenkins: (LIVE) gcp-gcby
   - Target Layer: 60-database
   - Action: destroy

3. **Apply:**
   - Jenkins: (LIVE) gcp-gcby
   - Target Layer: 60-database
   - Action: apply
   - 새로운 Cloud SQL이 PSC Endpoint 방식으로 생성됨

4. **검증:**
   ```bash
   # Private zone VM에서 접근 (성공)
   gcloud compute ssh gcby-gs01 --project=gcp-gcby
   mysql -h <PSC_ENDPOINT_IP> -u root -p

   # DMZ zone VM에서 접근 (실패 - 네트워크 격리)
   # 접근 시도 시 연결 타임아웃 발생
   ```

---

## 보안 개선 효과

### 3-tier 아키텍처 준수
✅ **DMZ zone (10.10.10.0/24):**
- 외부 노출 가능 영역
- Cloud SQL 접근 불가 (네트워크 레벨 격리)

✅ **Private zone (10.10.11.0/24):**
- 백엔드 애플리케이션
- Cloud SQL 접근 가능 (PSC Endpoint 통해)

✅ **mgmt VPC (10.250.10.0/24):**
- jenkins, bastion
- Cloud SQL 관리 접근 가능 (VPC Peering 통해)

### 네트워크 격리 방식
- **VPC Peering 방식:** 방화벽 규칙으로 제어 (우회 가능)
- **PSC Endpoint 방식:** 네트워크 레벨 격리 (우회 불가능)

---

## 기술 학습

### Cloud SQL Private Service Connect vs VPC Peering

**VPC Peering 방식:**
- `google_service_networking_connection` 사용
- 전체 VPC에서 접근 가능
- Subnet 레벨 제어 불가능
- IP 범위: GCP 자동 할당 (10.201.3.0/24)

**PSC Endpoint 방식:**
- `google_network_connectivity_service_connection_policy` 사용
- 지정한 Subnet에만 Endpoint 생성
- Subnet 레벨 격리 가능
- 다중 VPC 지원 (Cross-project, Cross-VPC)

### Service Connection Policy 구성
```hcl
resource "google_network_connectivity_service_connection_policy" "cloudsql_psc" {
  service_class = "gcp-cloud-sql"  # Cloud SQL 서비스

  psc_config {
    subnetworks = [
      "projects/gcp-gcby/regions/us-west1/subnetworks/gcby-subnet-private"
    ]  # Private subnet만 지정
    limit = 5  # 최대 엔드포인트 수
  }
}
```

---

## 관련 문서 업데이트

### 업데이트할 문서
1. ✅ `docs/changelog/work_history/2025-12-01.md` (이 파일)
2. 📝 `docs/changelog/CHANGELOG.md`
3. 📝 `docs/architecture/network-design.md`
   - DNS Peering 구조 추가
   - Cloud SQL PSC Endpoint 설명
   - 3-tier 격리 아키텍처

### 추가할 내용
- DNS Peering 설정 가이드
- Cloud SQL PSC Endpoint 전환 가이드
- 3-tier 네트워크 격리 베스트 프랙티스

---

## Git Commits

```
6463e4c feat: Cloud SQL을 PSC Endpoint 방식으로 전환
1c18375 feat: DB subnet을 전용 PSC 대역으로 전환
a74b413 feat: DNS Peering 설정 및 gcby VM DNS 레코드 추가
(여러 커밋) fix: 75-dns → 12-dns 변경 및 Phase 재편성
```

---

## 교훈

### ✅ 좋았던 점
1. **3-tier 아키텍처 완성:** DMZ → Private → DB 격리
2. **PSC Endpoint 활용:** 네트워크 레벨에서 근본적인 격리
3. **DNS Peering 구조:** mgmt에서 모든 프로젝트 VM 접근 가능
4. **SSH 태그 분리:** IAP와 mgmt VPC 접근 분리
5. **모듈 재사용성:** enable_psc 플래그로 VPC Peering/PSC 선택 가능

### ⚠️ 주의할 점
1. **Cloud SQL 재생성 필요:** VPC Peering → PSC 전환 시 다운타임 발생
2. **백업 필수 확인:** 재생성 전 반드시 백업 확인
3. **DNS Layer 위치:** 논리적 순서 고려 (Network 바로 다음)
4. **문서 조사:** PSC Endpoint 가능 여부 사전 확인 (Google 공식 문서)

### 🔍 추가 조사 필요
1. PSC Endpoint IP 범위는 어디서 할당되는가? (PSC 대역? Subnet CIDR?)
2. mgmt VPC에서 gcby Cloud SQL 접근 시 Endpoint IP는?
3. Cross-project Cloud SQL 접근 시 DNS 설정?

---

## 다음 작업 예정

1. ✅ work_history 작성 (이 파일)
2. 📝 CHANGELOG.md 업데이트
3. 📝 network-design.md 업데이트
4. 🔄 Jenkins apply 실행 및 검증
5. 📊 PSC Endpoint IP 확인 및 문서화
