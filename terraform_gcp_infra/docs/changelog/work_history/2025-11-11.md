# 작업 이력 - 2025-11-11

**작업자**: jsj  
**브랜치**: 433_code

---

## 🎯 작업 요약
Terragrunt 0.93 CLI로 전환하면서 Jenkins 파이프라인, 예제 파일, 문서를 전반적으로 갱신하고 새로운 실행 패턴(run/queue-include-dir/working-dir)을 통일했습니다.

---

## ✅ 완료된 작업
1. Jenkins 파이프라인 템플릿 및 jsj-game-k Jenkinsfile이 `terragrunt run --all`·`--working-dir`·`run --queue-include-dir` 패턴을 사용하도록 수정
2. Jenkinsfile 예제(.example)와 `run_terragrunt_stack.sh` 스크립트를 최신 CLI 사용 방식으로 업데이트
3. README/Quick Reference/Quick Setup/CHANGELOG/Jenkins Pipeline 가이드 등 모든 주요 문서에서 `run-all`/`--terragrunt-*` 표현 제거 및 새 명령 가이드 추가
4. Memorystore Redis 모듈/레이어 기본 버전을 `REDIS_6_X`로 조정하고, 지원 버전 검증 및 문서/예제(tfvars)를 업데이트
5. Terragrunt 루트 파일을 `root.hcl`로 리네임하고 모든 레이어/문서/스크립트에서 새로운 경로를 사용하도록 통일
6. 2025-11-11 작업 이력 추가 및 04_WORK_HISTORY.md에 요약 반영

---

## 🐛 해결한 이슈
- **Terragrunt run 명령 오류**: 0.93 버전에서 `run-all` 서브커맨드·`--terragrunt-include-dir` 플래그가 제거되면서 Jenkins가 연속 실패 → `run --all`, `--working-dir`, `run --queue-include-dir` 조합으로 재구성하고 `TG_NON_INTERACTIVE` 환경 변수 도입으로 비대화식 실행 보장
- **Redis 버전 오류**: Memorystore API가 `REDIS_7_X` 값을 거부해 apply 실패 → 모듈/레이어 기본값을 `REDIS_6_X`로 내리고 지원 버전만 허용하도록 validation 추가

---

## 🔗 관련 커밋
- `f7b4624` - `09e0fa6` - `Terragrunt run 명령 최신화 및 Jenkins 파이프라인 수정`

---

## 📚 참고 문서
- [terraform_gcp_infra/.jenkins/Jenkinsfile.template](../.jenkins/Jenkinsfile.template)
- [jenkins_docker/TERRAGRUNT_PIPELINE.md](../../jenkins_docker/TERRAGRUNT_PIPELINE.md)
- [05_quick setup guide.md](../05_quick%20setup%20guide.md)
