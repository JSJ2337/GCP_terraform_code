# 2025-12-05 작업 내역

## 주요 작업

### 1. proj-default-templet 전면 재구성
- gcp-gcby 환경을 기반으로 템플릿 재구성
- 플레이스홀더 패턴 적용:
  - `REPLACE_*`: root.hcl (state bucket, org_id, billing 등)
  - `YOUR_*`: common.naming.tfvars, Jenkinsfile
- 70-loadbalancers: gs만 유지 (app, lobby, web, example-http 제거)
- 50-workloads: workloads.tfvars로 파일명 변경

### 2. create_project.sh 개선
- 크로스 플랫폼 sed 지원 (`sedi()` 함수)
- Jenkins Credential ID 치환 추가
- 생성 후 수동 설정 필요 항목 안내 메시지 추가

### 3. 문서 정리 및 통합
- modules/*/README.md → docs/modules/*.md로 이전 (12개)
- scripts/README_SSH_VM.md → docs/scripts/ssh-vm-guide.md
- .jenkins/README.md → docs/guides/jenkins-template.md
- docs/examples/ 디렉토리 생성 및 tfvars.example 이전
- .terraform.lock.hcl 파일들 템플릿에 추가

### 4. 문서 업데이트
- CREATE_NEW_PROJECT.md: 현재 템플릿에 맞게 전면 재작성
- adding-new-project.md: Bootstrap 통합 고급 가이드로 축소
- 루트 README.md: 모듈 링크 수정, 프로젝트명 업데이트 (gcp-gcby)

## 변경된 파일

```
proj-default-templet/
├── root.hcl (REPLACE_* 플레이스홀더)
├── common.naming.tfvars (YOUR_* 플레이스홀더)
├── Jenkinsfile (YOUR_* 플레이스홀더)
├── 50-workloads/workloads.tfvars (신규)
└── 70-loadbalancers/gs/ (app, lobby, web 제거)

docs/
├── modules/ (신규 - 12개 모듈 문서)
├── scripts/ssh-vm-guide.md (이전)
├── guides/jenkins-template.md (이전)
├── examples/ (신규)
├── CREATE_NEW_PROJECT.md (업데이트)
└── README.md (업데이트)

scripts/create_project.sh (개선)
README.md (업데이트)
```

## 커밋 내역

1. `fix: create_project.sh sed 치환 로직 개선`
2. `refactor: proj-default-templet을 gcp-gcby 기반으로 전면 재구성`
3. `chore: .terraform.lock.hcl 파일 추가 및 example 파일 docs로 이전`
4. `docs: 문서를 현재 템플릿 구조에 맞게 업데이트`
5. `docs: modules README.md 파일들을 docs/modules/로 이전`
