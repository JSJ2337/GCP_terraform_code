# 작업 이력 - 2025-11-11 (Session 2)

**작업자**: Codex  
**브랜치**: 433_code

---

## 🎯 작업 요약
DMZ/Private/DB 서브넷 구조와 역할별 VM 구성을 정식화하고, Terragrunt 0.93 기준으로 모든 예제/문서를 동기화했습니다.

---

## ✅ 완료된 작업
1. **네트워크 레이어**
   - `subnet_primary_cidr`/`subnet_backup_cidr`/`pods_cidr`/`services_cidr`를 제거하고 `additional_subnets` + `dmz/private/db_subnet_name` 조합만으로 서브넷을 선언하도록 개편
   - tfvars·README를 모두 갱신해 DMZ/Private/DB self-link를 직접 입력하고 NAT을 DMZ에만 적용하는 패턴을 문서화
2. **워크로드 레이어 & 모듈**
   - `modules/gce-vmset`이 인스턴스별 `hostname`, `image_family/project`를 지원하도록 개선하고 `startup_script_file`을 통한 스크립트 재사용 패턴 도입
   - 템플릿/운영 tfvars·example에 역할별 instances map, Private/DMZ 서브넷 self-link, OS mix 예시 추가
   - 스크립트(`lobby.sh`, `was.sh`)를 `/usr/bin/env bash` + `set -euo pipefail` 표준으로 정비
3. **로드밸런서 & 문서 전반**
   - `internal_subnetwork_self_link` 사용 가이드를 README 및 tfvars.example에 추가해 내부 LB가 Private 서브넷을 보도록 안내
   - 00~05 README, Quick Reference, Layer/Module README, Jenkins 예제 등에서 jsj-game-k 기준으로 내용 업데이트 및 Terragrunt 0.93 문구 정리

---

## 🐛 해결한 이슈
- **Terragrunt run --queue-include-dir 오류**: `--queue-include-dir`가 글로벌 플래그로 처리되던 Jenkins 예제를 `run --queue-include-dir ...` 순서로 수정
- **GCE hostname/OS 커스터마이즈 불가**: `modules/gce-vmset`이 인스턴스별 이미지와 hostname을 받아들이지 못하던 문제를 per-instance data source/속성으로 해결

---

## 🔗 관련 커밋
- (작업 완료 후 커밋 예정) `feat: DMZ/Workload 업데이트`

---

## 📚 참고 문서
- `00_README.md`, `02_CHANGELOG.md`, `03_QUICK_REFERENCE.md`, `05_quick setup guide.md`
- `proj-default-templet/*/README.md`, `environments/LIVE/jsj-game-k/*/README.md`
