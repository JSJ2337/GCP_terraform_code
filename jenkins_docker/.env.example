# 환경 변수 설정 예시
# 사용 방법: 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# cp .env.example .env

# 호스트 사용자 UID/GID (권한 문제 방지)
# Linux/WSL에서는 'id -u' 및 'id -g' 명령으로 확인 가능
UID=1000
GID=1000

# ngrok Authtoken (ngrok.com에서 가입 후 발급)
# https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
